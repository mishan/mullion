{
  "//": "Only to check the declarations, which are hand-written: test/types.ts uses the API the way a consumer would, through the package's own exports map, so a name that moved or a shape that drifted is a failed build rather than a surprise in somebody else's editor.",
  "compilerOptions": {
    "strict": true,
    "noEmit": true,
    "target": "es2022",
    "lib": ["es2022", "dom"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "types": []
  },
  "files": ["test/types.ts"]
}
